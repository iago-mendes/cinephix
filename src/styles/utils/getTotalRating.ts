import styled from 'styled-components'

const Container = styled.div`
	display: flex;
	align-items: center;
	gap: 0.5rem;

	.stars {
		display: flex;
		align-items: center;
		gap: 0.25rem;
	}
`

export default Container
